import React from "react";

const EmptyChatThread = () => {
	return (
		<div className="flex items-center justify-center h-full text-muted-foreground">
			Select a conversation to start messaging
		</div>
	);
};

export default EmptyChatThread;
